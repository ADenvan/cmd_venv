


# test_run.md
- ![test_run.md](tests\test_run.md)
- Примечание:
    - Промпт рассчитан на использование в среде с установленным pytest, pytest-xdist (для -n auto) и опционально pytest-json-report.
    - Если агент не имеет доступа к shell, он должен эмулировать логику через pytest.main() с захватом sys.exit() и парсингом TestReport.









# Архитектура тестов
Составь архитектурный шаблон, со структурой заголовка для указаний и короткое описание. Мне нужен шаблон для тестов.

ЦЕЛЬ:
Найти все файлы `*_test.py` в каталоге, запустить `pytest -n auto --durations=0`.
Определить статусы (пройден/ошибка/пропуск) определить какая технология провалила ТЕСТЫ.

АЛГОРИТМ:
1. Проверьте наличие виртуальной среды: если файл `venv/Scripts/activate.bat` существует, используйте его.
2. Поиск: `**/*_test.py` в <directory_path> (исключить `__pycache__`, `.git`, `venv`, `node_modules`)
3. Выполните команду: `pytest -n auto --tb=short -v --durations=0` (в качестве запасного варианта: разбор стандартного вывода) с использованием виртуального окружения, если оно доступно.
4. Показатели: всего, пройдено, не пройдено, пропущено, xfail, процент покрытия
5. Анализ сообщений ImportError для извлечения имен отсутствующих пакетов.
6. Анализ времени выполнения тестов на основе выходных данных pytest.
7. Рассчитайте общее время выполнения всех тестов.
8. Для каждого теста рассчитайте процент времени выполнения: (время_теста / общее_время) * 100
9. Если обнаружены ошибки импорта, добавить в отчет нет pip install для отсутствующих пакетов.

ФОРМАТ ВЫВОДА:

ОГРАНИЧЕНИЯ:
- Без markdown-обертки (```json), чистый JSON



Промпт должен быть с ЧЕТКОЙ ЦЕЛЬЮ на запуск команды на тестирование всех файлов директории.
После запуска определить уровень прохождения тестов НАПРИМЕР: (зеленый, желтый, красный).
Собрав все данные в Один ОТЧЕТ, ВАЖНОЕ примечание отчет должен быть коротким но информативным для дольнего использование АГЕНТОМ.
А также важно! Перед написанием промпта задай мне уточняющие вопросы. Так мы получем боле качественный результат.
на данный момент я использую вот такие импорты (import pytest, import asyncio, import numpy as np, import sys, import os)
НЕ нужно делать: генерировать много информации (строк текста с размытым смыслом и четкой задачи)


